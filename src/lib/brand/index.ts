/**
 * Brand Profile & Brand Context — the shared brand layer.
 *
 * Import everything brand-related from `@/lib/brand`; there is exactly one
 * Brand Context implementation (`./context`) and one matching implementation
 * (`./match`), and no feature is allowed a private copy of either.
 */
export * from "./context";
export * from "./match";
