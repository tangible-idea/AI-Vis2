-- Demo data is seeded per-user from inside the app:
-- Settings → "Load demo project", or the "Try the demo project" option
-- during onboarding. This keeps rows owned by a real auth.users id so
-- RLS works without special cases. See src/lib/demo.ts.
select 1;
